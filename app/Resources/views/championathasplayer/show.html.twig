{% extends 'base.html.twig' %}

{% block body %}
    <h3>Championathasplayer</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ championatHasPlayer.getid }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ championatHasPlayer.getplace }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ championatHasPlayer.getpoints }}</td>
            </tr>
        </tbody>
    </table>

<a class="btn btn-default" href="{{ path('championathasplayer_index') }}">Back to the list</a>
<a class="btn btn-info" href="{{ path('championathasplayer_edit', { 'id': championatHasPlayer.getid }) }}">Edit</a>
{{ form_start(delete_form) }}
    <input class="btn btn-danger" type="submit" value="Delete">
{{ form_end(delete_form) }}
{% endblock %}
