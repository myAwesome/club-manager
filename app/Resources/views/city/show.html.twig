{% extends 'base.html.twig' %}

{% block body %}
    <h3>City</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>

<a class="btn btn-default" href="{{ path('city_index') }}">Back to the list</a>
<a class="btn btn-info" href="{{ path('city_edit', { 'id': city.id }) }}">Edit</a>
{{ form_start(delete_form) }}
    <input class="btn btn-danger" type="submit" value="Delete">
{{ form_end(delete_form) }}
{% endblock %}
